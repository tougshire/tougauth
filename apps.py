from django.apps import AppConfig


class TougshireAuthConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'tougshire_auth'
    verbose_name = 'Authorization and Authentication (Tougshire Auth)'
